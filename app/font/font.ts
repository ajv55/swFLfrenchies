import {Alex_Brush, Dancing_Script, Roboto } from 'next/font/google';

export const alex_brush = Alex_Brush({subsets: ['latin'], weight: ['400']});

export const dancing_script = Dancing_Script({subsets: ['latin'], weight: ['400']});

export const roboto = Roboto({subsets: ['latin'], weight:['400']})